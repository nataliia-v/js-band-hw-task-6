language: node_js
node_js:
  - 10.15.3
cache: npm

install:
  - yarn install

script:
  - yarn eslint
  - yarn build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master
